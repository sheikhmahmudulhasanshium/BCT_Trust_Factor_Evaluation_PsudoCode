for each para
    if there exist any sentence where BCT system can verify most of the part’s performance and fail to judge some parts, which problem is unsolved yet and has clear evidence that risk causing loss that means this system's performance verifiability is very low
        PV_Sum=PV_Sum+2
        PV_Count=PV_Count+1

        else if there exist any sentence where BCT system can verify most of the part's performance and fail to judge some parts, which problem is unsolved yet
            PV_Sum=PV_Sum+4
            PV_Count=PV_Count+1

        else if there exist any sentence where BCT If the BCT is somewhat better than other technologies, it should have a defined standard method that verifies the claim of performance and the superiority claim is technically legit and it has some ignorable limitations
            PV_Sum=PV_Sum+6
            PV_Count=PV_Count+1

        else if there exist any sentence where BCT is superior to other technology it should have a defined standard method that verifies the claim of performance and If the superiority claim is legit and it has some ignorable limitations
            PV_Sum=PV_Sum+8
            PV_Count=PV_Count+1

        else if there exist any sentence where BCT is superior to other technology it should have a defined standard method that verifies the claim of performance and the superiority claim is legit
            PV_Sum=PV_Sum+10
            PV_Count=PV_Count+1

    else 
        PV_Sum=PV_Sum+0
        PV_Count=PV_Count+0
PV=round(PV_Sum/PV_Count)
